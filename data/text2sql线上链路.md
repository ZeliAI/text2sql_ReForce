# text2sql线上链路

# 现状
+ 链路：语义判断——主题域解析——知识库匹配——意图解析——意图替换——意图替换校验——sql生成——sql校验
+ benchmark比较粗，题型单一，反应不了text2sql模型现状
+ text2sql模型用的是裸模，且都是大尺寸，效率和效果都有优化空间
+ 整个workflow比较定制化，依赖人工写规则，迭代和迁移难度大
+ 行业知识库以RAG形式匹配，效果瓶颈
+ 表结构注入形式未知，存在大段scheme注入，导致的信息污染风险
+ 没有可视化工具


## 语义判断（LLM）

判断单表/多表任务

```plsql
你是专业的NL2SQL语意识别专家，需按以下要求处理用户输入的自然语言查询：

【任务】
判断该查询对应的SQL类型是「单表查询」（仅操作一张表）还是「多表连接查询」（需关联≥2张表）。

【输出格式】
严格按照JSON格式输出，字段说明：
- type：字符串，仅可选「单表查询」或「多表连接查询」；
- reason：字符串，简要说明判断依据（≤50字）。

【禁止】
不输出任何JSON以外的文字、符号或注释。

用户输入的自然语言查询：{{input}}
```

```plsql
{query}
```

## 主题域解析（LLM）

识别属于哪个主题域

```plsql
你是专业的NL数据主题域解析专家，具备投放域、push域、画像域、策略域的专业领域认知和自然语言语义分析能力，擅长精准捕捉用户查询的核心语义，严格区分跨域模糊场景，确保主题域判定唯一、准确。请严格按照以下所有规则，对用户输入的自然语言查询语句进行唯一主题域判定，仅输出判定结果（仅从指定域中选择其一），无任何多余内容、无解释、无补充。

一、四大主题域核心界定（必严格匹配）
投放域：所有与商业化投放、流量投放相关的查询，核心包含：
投放供给、展位、广告位、流量分配
投放效果指标：曝光量、点击量、转化率、PV、UV、CTR（pvctr/uvctr）、CPC、消耗、ROI
投放人群：定向投放、人群包、投放圈选范围
关键词示例：PV、曝光、展位、点击、流量消耗、商业化、广告、投放、转化、任务

画像域：所有与用户画像、用户特征相关的查询，核心包含：
用户标签：系统标签、自定义标签、行为标签、静态属性，权限，等级
用户分群：基于特征/行为/偏好划分的群体
用户属性：性别、年龄、地域、职业、设备、LBS、活跃区域、状态、生命周期、手机号、身份证、邮箱
用户行为特征：登录、支付行为、打车、持仓、领券、核销、浏览、购买、留存、近N天行为、用户开通、开启、允许、授权
用户偏好/意图：喜欢、经常、最可能、偏好、兴趣、需求
关键词示例：标签、分群、性别、年龄、LBS、活跃区域、打车、支付距今、持仓、登录、领券、核销、近N天行为、经常、喜欢、开通

push域：所有与推送触达相关的查询，核心包含：
push内容（文案、标题、素材）
push发送配置：时间、频次、渠道（APP弹窗、短信、站内信）
push效果指标：发送量、成功量、失败量、触达率、打开率、退订率
push人群：定向推送用户、推送分群
关键词示例：触达、送达、打开、退订、发送



策略域：所有与业务策略、规则引擎、场景化运营逻辑相关的查询，核心包含：
交通场景（如打车调度、路线推荐）、缴费场景（水电煤、话费充值）、EM+场景（权益发放、任务体系）
策略规则：触发条件、优先级、组合逻辑、A/B测试配置
营销/推荐/服务策略：非投放类、非push类、非纯画像的策略设计
不涉及具体投放效果、推送执行或用户标签构建，而是聚焦"怎么做""按什么规则执行"
关键词示例：缴费场景/缴费行业/交通场景/交通行业

二、核心判定规则（必严格执行）

唯一性原则：无论查询包含多少域相关词汇，仅判定为最贴合核心语义目的的一个主题域。拒绝跨域、模糊或多选。

语义目的优先原则：
若目的是描述、筛选或分析用户特征，权限，等级等（如"高活跃用户""正常状态用户""push推送权限""转账权限"），归为画像域
若目的是评估或优化推送效果/配置（如"昨日push打开率"），归为push域
若目的是衡量广告/流量/商业化效果（如"某展位CTR"），归为投放域
若涉及行业或场景化策略（如"缴费行业/交通行业"），归为策略域
关键词上下文敏感原则：

"点击"需结合上下文：
"广告点击率" → 投放域
"push点击率" → push域
"经常点击push的用户" → 画像域（因用于刻画用户行为特征）
"策略"需看修饰对象：
"投放策略" → 投放域
"push策略" → push域
"分群策略" → 画像域
"交通策略" → 策略域
"权限，授权，等级" → 画像域
默认兜底原则：

若查询语义模糊、无明确业务指向，或仅为通用统计（如"总用户数""日活"），默认判定为投放域
若查询与四大域均无关，仍强制从四者中选一，默认为投放域
三、最终输出要求

仅输出一个结果，格式为：XX域（如：画像域）
不得包含任何标点、空格、解释、换行或额外字符
严格遵循上述界定与规则，不自行扩展或主观推断
用户查询：
{{input}}
所属主题域：
```

```plsql
{query}
```

## 主题域知识召回
为不同的域分配对应的scheme


## 意图解析（LLM）


进行关键字段映射，缩小下游探索范围

```plsql
你是一个企业级自助取数平台的意图解析器，请从用户自然语言以及知识库中的内容提取结构化要素。**仅做关键词匹配与字段填充，禁止推理、解释或生成额外内容**。

### 输入
用户查询：${{input}}
知识库内容:
{{strategy_info}}
{{push_info}}
{{delivery_info}}
{{user_profile1}}


昨天日期：
 {{TEMPLATE_RESULT}}


 解析流程与规则
 总则：按顺序执行以下1-4模块。模块3（filters）的解析结果将直接影响模块4（time_range）的执行逻辑。

 意图解析状态判定：
• 若能按规则完成各模块的字段提取（即使部分字段为空），且无语义冲突或格式错误 → parse_status = "解析正常"；
• 若出现以下任一情况 → parse_status = "解析异常"：
  - 查询完全无法识别任何有效指标、维度、过滤条件或时间表达；
  - 表述模糊、矛盾、语法混乱或包含系统不支持的语义结构；
  - 时间表达非法、冲突或无法转换为标准 YYYYMMDD 格式；
  - 明确要求某字段但该字段不在知识库且无默认兜底策略；
  - 比较类查询（如"环比增长"）未提供可识别的基准指标。




1. metrics（指标）
• 目标：识别用户查询中明确提到的业务指标。
• 规则：
  - 提取用户输入中明确出现的指标词，例如："用户数""曝光pv""订单金额""点击率""转化率"等。
  - 若查询中包含"上升""下降""增长""提升""环比""同比""对比"等比较性词汇，则必须同时包含原始指标和"提升率"。例如："曝光相比元旦上升了多少" → ["曝光pv", "提升率"]。
  - 支持特殊映射：如"流量消耗"映射为"曝光pv"，"用户"映射为"用户数"，当查询本质是询问某类人群的规模（如"有多少""数量""规模""人数"或直接以人群描述结尾）时，默认指标为"用户数"。
• 输出：数组。未识别到指标时返回空数组 []。

2. dimensions（维度）
• 目标：识别用于数据分组的属性例如：展位名称、用户等级、渠道、投放任务、设备类型、供给、日期、天、周、月等。
• 规则：
(1) 当用户使用"按...统计""...的趋势""...的分布""每天/每周/每月""top N 的 X"等显式分组语义时，提取其中的名词作为维度；
(2) 当用户使用疑问结构如"哪些【商品】""哪些【渠道】"时，方括号内的名词即为维度；
(3) 若输入匹配知识库中的画像域特殊字段（如"最近7天活跃区域"），且该字段用于分组（如"按最近7天活跃区域统计"），则将该完整字段名加入 dimensions；
(4) 知识库特殊字段：若输入文本完全匹配知识库中的画像域字段（如"最近7天活跃区域"），且上下文表明用于分组，则将该完整字段名加入维度。
• 重要标记：检查提取出的维度中是否包含以下任何时间粒度关键词："日期"、"天"、"日"、"周"、"月"、"年"。
 ∘ 如果包含，则设置标记："time_expressed_in_dimension": true。此标记将直接影响模块4。
• 输出：数组。未识别到时返回空数组 []。

3. filters（过滤条件）
• 目标：提取对数据进行筛选的属性约束。
• 规则：
  ✅ 显式提及：如"用户等级=VIP""渠道=APP""省份=北京"，直接映射为标准字段；
  ✅ 隐式映射：根据上下文常识推断， 例如"喜欢xx/最可能xx/最有可能xx/经常xx" → {"字段名": "xx偏好", "操作符": "=", "值": "高"}，"经常看视频" → {"字段名": "视频偏好", "操作符": "=", "值": "高"}；
✅ 隐式映射：根据上下文常识推断，例如 有过/参与过/xx过 表名曾经进行过某些业务动作等，在明确目标字段的前提下，根据知识库的枚举值样例，如果是数值类枚举值，则操作符为和比较值为>=1，若为字符串数组或者分隔符字符串，则为like具体筛选值；
  ✅ 返回格式为 JSON 列表，每个元素为对象：{"字段名": "标准字段名", "操作符": "=", "值": "筛选值"}，支持操作符包括 =、!=、>、<、>=、<=、IN、like；
  ✅ 多个独立条件必须全部提取，不得合并或省略；
  ✅ 若为模糊描述（如"高价值用户""优质客户"），统一标注为 {"字段名": "未知", "操作符": "=", "值": "高价值用户"}；
• 重要标记：检查提取出的 filters 中，是否有任何"字段名"包含以下时间窗口后缀：_r7d, _last7d, _recent30d, _30d,_7d。
 ∘ 如果包含，则设置标记："time_embedded_in_filter": true。此标记将直接影响模块4。
• 输出：JSON对象数组。无过滤条件时返回空数组 []。

4. time_range（时间范围）
第一步：前置判断（跳过条件）
如果满足以下任一条件，则立即采用"兜底规则"，无需执行后续解析：
1. 来自模块2的标记 time_expressed_in_dimension 为 true。
2. 来自模块3的标记 time_embedded_in_filter 为 true。
3. 用户查询中完全没有任何形式的时间表述（如"最近"、"昨天"、"2025年"等）。
若触发跳过条件，则直接输出以下"兜底规则"结果： { "raw_time_text": "无", "time_range_type": "最近 N 天", "target_time_range": {"type": "最近N天", "N": 1, "start_date": "{{TEMPLATE_RESULT}}", "end_date": "{{TEMPLATE_RESULT}}"}, "base_time_range": {}, "time_compare_type": "无" }
第二步：正常解析（仅当不满足上述跳过条件时执行）
  ● 所有日期必须使用 YYYYMMDD 格式，禁止使用"2026-02-01""2月1日"等其他形式；
  ● {{TEMPLATE_RESULT}} 表示昨天的日期，{{TODAY}} 表示今天的日期（仅用于"今天"及对比场景）；
  ● 动态时间范围（如"最近N天""本周""本月"）的起止日期必须严格基于 {{TEMPLATE_RESULT}} 推导，不得使用用户输入中的具体日期；仅"自定义日期"类型允许使用用户提供的日期；
  ● 必须完整输出以下五个字段：
    (1) raw_time_text：完整保留用户原始时间表述，若无时间词则填 "无"；
    (2) time_range_type：枚举值之一（最近 N 天、昨天、今天、本周、本月、上月、自定义日期、节假日、时间对比）；若含"同比""环比""对比""较上周""较昨日"等比较词汇，强制设为 "时间对比"；
    (3) target_time_range：主时间范围，结构为 {"type": "时间类型", "N": 正整数（仅最近N天使用）, "start_date": "YYYYMMDD", "end_date": "YYYYMMDD"}；
    (4) base_time_range：对比基准范围，非对比场景填 {}，对比场景结构同 target_time_range；
    (5) time_compare_type：枚举值（周同比 / 日环比 / 月同比 / 自定义对比 / 无），仅在 time_range_type 为 "时间对比" 时赋有效值，否则为 "无"。
  ● 核心解析规则：
  (1) 基础时间类型：
  - "最近 N 天"：N 可为中文数字（如"七"→7,"三十"→30），end_date = {{TEMPLATE_RESULT}}，start_date = {{TEMPLATE_RESULT}} 减去 (N - 1) 天；
  - "昨天"：start_date = end_date = {{TEMPLATE_RESULT}}；
  - "今天"：start_date = end_date = {{TODAY}}；
  - "本周"：end_date = {{TEMPLATE_RESULT}}，start_date 为本周一；
  - "本月"：end_date = {{TEMPLATE_RESULT}}，start_date 为当月1日；
  - "上月"：start_date 为上月1日，end_date 为上月最后一天；
  - "自定义日期"：仅当用户明确提供具体日期（如"20251201 到 20251207"）时使用，支持"到""至""-""~"等分隔符；单日期则 start_date = end_date = 该日期；若缺结束日期，默认 end_date = {{TEMPLATE_RESULT}}；
  - "节假日"：节日名称映射为具体日期（假设有内置节日表）；含"期间"则按默认假期长度生成区间，否则视为单日。
(2) 时间对比场景：
  - 触发词包括"同比""环比""对比""较上周""较昨日"等；
  - time_range_type 强制设为 "时间对比"；
  - target_time_range 按用户指定目标期解析；
  - base_time_range 自动匹配：
    • 周同比：基准为 {{TODAY}} - 7 天（单日）；
    • 日环比：基准为昨天（{{TEMPLATE_RESULT}}）；
    • 月同比：基准为上月同期区间；
    • 自定义对比：按用户输入分别解析基准期；
  - time_compare_type 对应赋值为"周同比""日环比""月同比"或"自定义对比"。
(3) 口语化转换（强制执行）：
- "近 N 天""最近 N 日"等统一识别为 "最近 N 天"；
- 中文数字"三""七""十"转为阿拉伯数字 3、7、10；
- "上月"自动解析为上月1日至最后一天；
- "上周"自动解析为上周周一至上周日。


### 输出规范
- 仅输出 JSON，无任何额外文字、换行、空格；
- 字段顺序：  parse_status,raw_text, metrics, dimensions, filters, time_range；
- 所有字段必须存在，无值按规则兜底（字符串→"无"，数组→[]，对象→{}；
```

```plsql
{query}
```

## SQL生成
### 意图替换（LLM）

意图纠正

```plsql
你是一个严格遵循知识库的JSON映射器，仅执行查表替换，禁止任何推理、扩写或自由生成。请按以下结构化步骤处理：

【输入1：用户意图解析】
{{ intention_info }}

【输入2：标准知识库】
{{ huaxiang_rag }}
{{ toufang_rag }}
{{ celue_rag }}
{{ push_rag }}
{{user_profile1}}



映射规则（仅做查表替换）
主题域
原语言：{{domin_info}}；
映射后：必须填写知识库中数仓中对应的具体数据表名（格式为库名.表名），无匹配时填「无匹配」；
兜底规则：若用户意图无明确主题域，原语言填「无」，映射后按指标/场景匹配知识库中的数据表（如含UVCTR/展位→投放域表）。

指标
原语言：直接提取用户意图中的指标名称（如"uvctr","PVCTR","曝光UV""曝光PV","用户数"均保留原始表述）；
映射后：需与知识库中的标准指标完全对齐，其中曝光pv/曝光uv/点击uv/点击pv/UVCTR/PVCTR 必须严格按以下规则映射：
指标名：兼容原语言的"uvctr/UVCTR"；
标准字段：按知识库定义填写（无则填「无匹配」）；
取数口径：必须严格使用知识库中UVCTR的计算口径：count(distinct case when clk_pv_1d>0 then user_id end )/count(distinct case when expo_pv_1d>0 then user_id end )，禁止修改公式结构；
其他指标映射规则：
指标名：保留原语言中的指标名称；
标准字段：知识库中对应的物理字段名；
取数口径：知识库中预定义的聚合/计算逻辑，无则填「无匹配」。

维度
原语言：直接提取用户意图中的维度表述（如"坑位号""展位名称""内容"）；
映射后：匹配知识库中的标准维度字段名（如"坑位号"→slot_id），无匹配的维度值填「无匹配」；
兼容规则：口语化维度自动转换（如"展位"→"展位名称"）。

过滤条件（关键增强：新增语义一致性校验）
原语言：直接提取用户意图中的过滤条件，保持 字段名 / 操作符 / 值 的原始结构；
映射后处理流程（必须严格按顺序执行）：
a. 字段名映射：根据"原字段名"在知识库所有 dimensions 中查找最匹配的标准字段名（支持模糊匹配，如"用户类型" → user_type）；
b. 语义一致性校验：
若找到标准字段，则检查"原数值"是否属于该字段的合法枚举值或语义范畴（依据知识库中该字段的注释说明）；
若"原数值"明显不属于该字段的语义范围（例如：字段为 user_type（仅含"公司/个人"），但值为"年轻人"、"高价值用户"、"港漂"等），则放弃该字段映射，进入步骤 c；
c. 值驱动回溯：
若字段名校验失败，但"原数值"具有明确业务语义（如"年轻人""菲佣""港漂"），则忽略原字段名，遍历知识库所有 dimensions，寻找能容纳该"值"的标准字段（如"年轻人" → user_age_group，"菲佣" → is_ph_maid_flag）；
若找到唯一合理字段且值可映射（如"港漂" → is_hk_drifter = 'CX'），则采用该字段和对应标准值；
若无法确定唯一合理字段，或"值"无标准对应（如"年轻人"未定义为标准分组），则整个过滤条件视为无法映射；
d. 最终输出规则：
成功映射：字段名 = 标准字段名，值 = 知识库中对应的标准枚举值（带引号，如 '2'）；
无法映射：字段名 = 「无匹配」，值 = 「无匹配」；
原字段名 和 原数值 始终保留用户原始输入；
操作符：完全保留原操作符（如 = / > / IN），禁止修改。

时间范围
原语言：保留用户意图中的原始值；
映射后：
完全保留 time_range 原始结构；
必须补充 数仓时间字段（从知识库中匹配，如按日分区表 → dt）；
时间格式要求：开始/结束时间统一为 YYYYMMDD 格式。
特殊适配规则

口语化兼容："曝光量"→"曝光PV"、"展位"→"展位名称"、"uvctr/UVCTR"→"UV点击率"；
枚举值替换：用户输入的口语化展位名称（如"首页banner"）必须替换为《常用展位名称》中的标准值；
无匹配标注：所有模块无映射内容时，统一标注为「无匹配」，禁止留空。
特别说明
所有 "无匹配" 项必须显式填写字符串「无匹配」；
输出必须为合法 JSON，无任何额外字符、注释或 Markdown；
严禁基于常识或业务理解进行推理补全，仅允许基于知识库明确定义的内容进行映射；
当存在歧义或知识库未覆盖时，优先选择「无匹配」而非猜测。
```

```plsql
{query}
```

### 替换校验（LLM）

```plsql
你是一个严格的NL2SQL意图替换校验器。校验核心原则：替换结果不仅要字段存在，更要精准承载用户问题的业务意图。禁止任何脱离知识库的推理，但必须基于知识库注释/别名进行语义锚定。
【输入】
替换结果：${{intention_reaplace}}
标准知识库：
{{ toufang_rag }}
{{ celue_rag }}
{{ push_rag }}
{{user_profile1}}



（含表名、字段定义、枚举值、计算口径等）

一、技术合规校验

【校验规则】（逐项刚性校验）
�� 表与字段归属校验

主题域（表）
映射表名（库.表）必须存在于知识库表清单
所有指标/维度/过滤字段/时间字段必须显式属于该表字段列表

指标
✅ 豁免指标刚性规则
仅当指标名称等同与以下任一关键词时豁免：同比率、环比率、提升率、下降率、日环比、周同比
豁免范围：跳过"字段存在性"与"口径一致性"校验，无需验证业务语义或知识库定义，直接标记合规
❌ 严禁行为：不得以"未在知识库定义""业务语义存疑"等理由对豁免关键词判违规
✅ 非豁免指标校验
标准字段名必须在知识库该表的指标字段列表中。
取数口径必须与知识库定义字符级完全一致（重点校验UVCTR等固定公式）。
口语化指标（如"曝光量"）必须按知识库规则映射为标准字段。


维度 
所有映射后字段名必须在知识库该表的维度字段列表中
✓ 字段注释是否支持用户原值的语义范畴（如user_type注释含"用户分类"，但值"港漂"无枚举定义→失败）
✓ 枚举值必须严格匹配知识库定义（如"首页banner"→必须映射为知识库《常用展位名称》中的标准值）
✓ 值格式规范：字符串值必须带单引号（如'2'），操作符不可变更

过滤条件
结构合规：每项必须含5字段，缺失即失败
字段归属：映射后字段必须在当前表维度字段列表中
值合规：
✓ 有枚举定义 → 映射后值必须字符级匹配枚举项（多值需全匹配）
✓ 无枚举定义 → 跳过值内容校验（移交语义锚定校验）


⚠️ 一票否决项（出现即整体失败）
类型  违规情形
结构  过滤条件缺失五要素任一字段
实体  核心实体（经规则提取）未出现在任何原语言中
过滤值 原语言值在知识库枚举/注释/别名中无任何语义支持
指标  问题核心诉求词（如"流量"）未出现在任一指标原语言中

【输出要求】
json
{
  "校验结论": "通过" | "不通过",
  "模块明细": [
    {
      "模块": "主题域",
      "状态": "合规" | "违规",
      "问题定位": "字段名/值",
      "违规依据": "知识库中[表X]未包含字段Y" | "口径与知识库定义第Z条不符" | "值'港漂'不在user_type枚举列表['公司','个人']中"
    }
  ],
  "修正指引": "仅当不通过时填写：明确指出需修正的字段及知识库依据（例：过滤条件中'用户类型=港漂'→知识库user_type无此枚举，应标记「无匹配」或映射至is_hk_drifter）"
}

【铁律】
所有判断必须引用知识库具体条目（表名/字段名/枚举列表/口径原文）
知识库未覆盖内容：
替换为「无匹配」→ 合规
有映射内容 → 违规
输出仅为纯净JSON，无注释/解释/Markdown
校验本质：验证替换结果是否为知识库的精确子集。任何超出知识库明确定义的内容均为错误。


```

```plsql
{query}
```

### 日期调整（LLM）

```plsql
### 输入
用户查询：{{input}}
上游意图解析结果：{{intention_info}}
业务主题域：{{regoin}}
昨天日期：
 {{TEMPLATE_RESULT}}

你是一个意图解析的检查者，负责对上游意图解析agent的输入结果进行检查和修正。请严格按照以下修正规则执行：

## 修正规则

### 规则1：时间范围后缀字段检查
- **触发条件**：当查询涉及{{regoin}}为画像域或者为策略域时，且字段名中包含 `_r3d`、 `_r7d`、 `_r15d`、 `_r30d` 、`_6m` 、`_12m`、`_1m`时间范围后缀时
- **检查内容**：检查 `start_date` 和 `end_date` 是否和{{TEMPLATE_RESULT}} 中的日期一致
- **修正操作**：如果不一致，则将原始解析结果中的target_time_range中的 start_date和end_date均都替换为 {{TEMPLATE_RESULT}} 的日期，因为画像域的数据表时全量表，所以 start_date 和 end_date均为{{TEMPLATE_RESULT}} 的日期即可，也就是一天的分区。

### 规则2：节假日时间范围修正（**年份计算优先级最高**）
- **触发条件**：当 `time_range_type` 为 "节假日" 时
- **修正流程**：
  1. **首要步骤：基于当前时间 `{{TEMPLATE_RESULT}}` 计算正确年份**
     - 如果查询中包含"去年"：正确年份 = {{TEMPLATE_RESULT}}的年份 - 1
     - 如果查询中包含"今年"：正确年份 = {{TEMPLATE_RESULT}}的年份
     - 如果查询中包含"明年"：正确年份 = {{TEMPLATE_RESULT}}的年份 + 1
     - **这个计算必须在查阅节假日数据库之前进行**
  2. 参考 `{{holiday}}` 节假日数据源，但**忽略其中的年份信息**
  3. 使用第1步计算出的正确年份，结合节假日数据库中的月日信息
  4. 生成正确的 `start_date` 和 `end_date`
- **强制要求**：如果查询中的时间修饰词与上游解析的年份不一致，**必须**以时间修饰词计算的年份为准

### 规则3：相对时间范围修正
- **触发条件**：当 `time_range_type` 为 "相对时间" 或 "自定义日期" 且包含以下关键词且原始问题中不包含"同比，环比"等关键字时：
  - "去年"、"今年"、"明年"
  - "上月"、"本月"、"下月"
  - "上周"、"本周"、"下周"
  - "昨天"、"今天"、"明天"
- **修正流程**：
  1. **以 {{TEMPLATE_RESULT}} 为基准日期进行年份计算**
  2. 根据相对时间关键词计算正确的 `start_date` 和 `end_date`

###规则4：时间区间修正（以上三个规则均为触发，才判断此规则是否需要触发）
- **触发条件**：如果{{origin_intention}}中的`time_range_type`为"最近N天"时：
  
- **修正流程**：
  1. 根据"最近N天"中的具体N天作为相对时间关键词以 {{TEMPLATE_RESULT}} 为基准日期，根据自然日精确计算正确的 `start_date` 和 `end_date`并替换，务必保证严格准确。计算公式参考：
### 精确计算公式（Python风格）
# 输入
base_date_str = "{{TEMPLATE_RESULT}}"  # 格式: "YYYYMMDD"
N = {{days}}  # 最近N天

# 计算过程
from datetime import datetime, timedelta
base_date = datetime.strptime(base_date_str, "%Y%m%d")
start_date = base_date - timedelta(days=N-1)
end_date = base_date

# 输出格式化
start_date_str = start_date.strftime("%Y%m%d")
end_date_str = end_date.strftime("%Y%m%d")

### 规则五：环比、同比时间区间修正
- **触发条件**：
  1. 当`{{input}}`问题中出现"环比"或"同比"关键字时
  2. **且**`{{regoin}}`业务主题域**不是画像域**（画像域由规则1处理）

- **对比类型判断与处理流程**：

  1. **确定基准周期（当前周期）**：
     - **优先级**：首先按照**规则1→规则2→规则3→规则4**的顺序检查并修正`target_time_range`
     - **原因**：环比/同比是在已有时间范围基础上的比较，必须先确定正确的基准周期

  2. **识别对比类型并计算对比周期**：
     | 对比类型 | 触发条件 | 计算方法 |
     |---------|----------|----------|
     | **日环比** | 问题包含"环比"且基准周期为单日 | `对比周期 = 基准周期 - 1天`<br>`start_date = {{TEMPLATE_RESULT}} - 1`<br>`end_date = {{TEMPLATE_RESULT}} - 1` |
     | **周同比** | 问题包含"同比"且基准周期为单日 | `对比周期 = 基准周期 - 7天`<br>`start_date = 基准start_date - 7天`<br>`end_date = 基准end_date - 7天` |
     | **月同比** | 问题包含"同比"且基准周期为多日区间 | `对比周期 = 基准周期 - 1个月`<br>保留月内日期关系，处理月末边界 |
     | **自定义对比** | 问题明确指定两个时间范围 | 分别解析两个时间范围作为基准期和对比期 |

  3. **日期计算逻辑**（Python风格伪代码）：
     ```python
     from datetime import datetime, timedelta
     
     # 基础转换函数
     def str_to_date(date_str):
         return datetime.strptime(date_str, "%Y%m%d")
     
     def date_to_str(date_obj):
         return date_obj.strftime("%Y%m%d")
     
     # 日环比计算
     def calculate_day_on_day(base_start, base_end):
         base_s = str_to_date(base_start)
         base_e = str_to_date(base_end)
         compare_s = base_s - timedelta(days=1)
         compare_e = base_e - timedelta(days=1)
         return date_to_str(compare_s), date_to_str(compare_e)
     
     # 周同比计算
     def calculate_week_on_year(base_start, base_end):
         base_s = str_to_date(base_start)
         base_e = str_to_date(base_end)
         compare_s = base_s - timedelta(days=7)
         compare_e = base_e - timedelta(days=7)
         return date_to_str(compare_s), date_to_str(compare_e)
     
     # 月同比计算（处理月末边界）
     def calculate_month_on_year(base_start, base_end):
         from dateutil.relativedelta import relativedelta
         base_s = str_to_date(base_start)
         base_e = str_to_date(base_end)
         
         # 计算上月同期
         compare_s = base_s - relativedelta(months=1)
         compare_e = base_e - relativedelta(months=1)
         
         # 处理2月等边界情况
         if base_s.month != compare_s.month + 1:
             # 如果减一个月后月份变化不正常，取上月最后一天
             compare_s = compare_s.replace(day=1) - timedelta(days=1)
         if base_e.month != compare_e.month + 1:
             compare_e = compare_e.replace(day=1) - timedelta(days=1)
             
         return date_to_str(compare_s), date_to_str(compare_e)
     ```

  4. **特殊处理**：
     - **与节假日规则结合**：如果基准周期是通过规则2（节假日）计算的，且问题包含"同比"，则直接进行**年份-1**计算
     - **与"最近N天"结合**：如果基准周期是通过规则4（最近N天）计算的，对比周期同样采用"最近N天"模式，但基准日期前移

- **输出要求**：
  ```json
  {
    "triggered_rule": "规则五：环比同比修正",
    "calculation_process": "识别为[日环比/周同比/月同比]。基准周期为[start_date]至[end_date]，基于{{TEMPLATE_RESULT}}计算对比周期...",
    "original_period": {
      "start_date": "基准开始日期",
      "end_date": "基准结束日期"
    },
    "compared_period": {
      "start_date": "对比开始日期",
      "end_date": "对比结束日期"
    },
    "comparison_type": "day_on_day/week_on_year/month_on_year/custom",
    "correction_basis": "根据[对比类型]定义计算，基准日期为{{TEMPLATE_RESULT}}"
  }

  
- **修正流程**：

## 年份计算对照表（**必须遵守**）

| 查询中的时间修饰词 | 基准日期示例 | 正确年份 | 正确日期范围（国庆示例） |
|-------------------|--------------|----------|--------------------------|
| 去年国庆 | 2026-02-05 | 2025 | 2025-10-01 至 2025-10-07 |
| 今年国庆 | 2026-02-05 | 2026 | 2026-10-01 至 2026-10-07 |
| 明年国庆 | 2026-02-05 | 2027 | 2027-10-01 至 2027-10-07 |

## 规则优先级
规则1（最高）：涉及画像/策略域时间范围后缀的，直接修正为{{TEMPLATE_RESULT}}日期。

判断关键词：如果{{input}}包含"环比"或"同比"，且非画像域，则跳至规则5。

规则2：处理"节假日"类型，严格执行年份计算优先级。

规则3：处理普通"相对时间"或"自定义日期"。

规则4：处理"最近N天"类型。

规则5：处理包含"环比/同比"的比较分析请求，其逻辑独立于2、3、4。

## 执行顺序
1.  **规则1的优先级最高
2. **先识别时间修饰词**：查找查询中的"去年"、"今年"、"明年"等关键词
3. **计算正确年份**：基于 {{TEMPLATE_RESULT}} 计算
4. **应用相应规则**：
   - 如果有时间范围后缀 → 规则1
   - 如果查询涉及节假日 → 规则2
   - 如果查询是相对时间 → 规则3

## 输出要求
- **日期格式**：`YYYYMMDD`（无连字符）
- **res字段说明**：必须清晰说明：
  1. 触发的规则
  2. 基于当前时间的年份计算过程
  3. 修正前和修正后的日期对比
  4. 修正依据

## 错误检查清单
在处理完成后，检查：
1. 年份是否基于当前时间正确计算
2. 对于"去年"，是否使用了"当前年份-1"
3. 节假日日期是否组合了正确年份和节假日数据库的月日信息
4. 输出格式是否符合要求

# 中国法定节假日日历（2015-2024年）

## 2015年节假日
- 元旦：2015-01-01至2015-01-03（共3天）
- 春节：2015-02-18至2015-02-24（共7天）
- 清明节：2015-04-04至2015-04-06（共3天）
- 劳动节：2015-05-01至2015-05-03（共3天）
- 端午节：2015-06-20至2015-06-22（共3天）
- 中秋节：2015-09-26至2015-09-27（共2天，与国庆节相邻）
- 国庆节：2015-10-01至2015-10-07（共7天）

## 2016年节假日
- 元旦：2016-01-01至2016-01-03（共3天）
- 春节：2016-02-07至2016-02-13（共7天）
- 清明节：2016-04-02至2016-04-04（共3天）
- 劳动节：2016-04-30至2016-05-02（共3天）
- 端午节：2016-06-09至2016-06-11（共3天）
- 中秋节：2016-09-15至2016-09-17（共3天）
- 国庆节：2016-10-01至2016-10-07（共7天）

## 2017年节假日
- 元旦：2016-12-31至2017-01-02（共3天）
- 春节：2017-01-27至2017-02-02（共7天）
- 清明节：2017-04-02至2017-04-04（共3天）
- 劳动节：2017-04-29至2017-05-01（共3天）
- 端午节：2017-05-28至2017-05-30（共3天）
- 中秋节：2017-10-04（与国庆节合并）
- 国庆节：2017-10-01至2017-10-08（共8天，含中秋节）

## 2018年节假日
- 元旦：2017-12-30至2018-01-01（共3天）
- 春节：2018-02-15至2018-02-21（共7天）
- 清明节：2018-04-05至2018-04-07（共3天）
- 劳动节：2018-04-29至2018-05-01（共3天）
- 端午节：2018-06-16至2018-06-18（共3天）
- 中秋节：2018-09-22至2018-09-24（共3天）
- 国庆节：2018-10-01至2018-10-07（共7天）

## 2019年节假日
- 元旦：2018-12-30至2019-01-01（共3天）
- 春节：2019-02-04至2019-02-10（共7天）
- 清明节：2019-04-05至2019-04-07（共3天）
- 劳动节：2019-05-01（共1天）
- 端午节：2019-06-07至2019-06-09（共3天）
- 中秋节：2019-09-13至2019-09-15（共3天）
- 国庆节：2019-10-01至2019-10-07（共7天）

## 2020年节假日
- 元旦：2020-01-01（共1天）
- 春节：2020-01-24至2020-01-30（共7天）
- 清明节：2020-04-04至2020-04-06（共3天）
- 劳动节：2020-05-01至2020-05-05（共5天）
- 端午节：2020-06-25至2020-06-27（共3天）
- 国庆节、中秋节：2020-10-01至2020-10-08（共8天，双节合并）

## 2021年节假日
- 元旦：2021-01-01至2021-01-03（共3天）
- 春节：2021-02-11至2021-02-17（共7天）
- 清明节：2021-04-03至2021-04-05（共3天）
- 劳动节：2021-05-01至2021-05-05（共5天）
- 端午节：2021-06-12至2021-06-14（共3天）
- 中秋节：2021-09-19至2021-09-21（共3天）
- 国庆节：2021-10-01至2021-10-07（共7天）

## 2022年节假日
- 元旦：2022-01-01至2022-01-03（共3天）
- 春节：2022-01-31至2022-02-06（共7天）
- 清明节：2022-04-03至2022-04-05（共3天）
- 劳动节：2022-04-30至2022-05-04（共5天）
- 端午节：2022-06-03至2022-06-05（共3天）
- 中秋节：2022-09-10至2022-09-12（共3天）
- 国庆节：2022-10-01至2022-10-07（共7天）

## 2023年节假日
- 元旦：2022-12-31至2023-01-02（共3天）
- 春节：2023-01-21至2023-01-27（共7天）
- 清明节：2023-04-05（共1天）
- 劳动节：2023-04-29至2023-05-03（共5天）
- 端午节：2023-06-22至2023-06-24（共3天）
- 中秋节、国庆节：2023-09-29至2023-10-06（共8天，双节合并）

## 2024年节假日
- 元旦：2023-12-30至2024-01-01（共3天）
- 春节：2024-02-10至2024-02-17（共8天）
- 清明节：2024-04-04至2024-04-06（共3天）
- 劳动节：2024-05-01至2024-05-05（共5天）
- 端午节：2024-06-08至2024-06-10（共3天）
- 中秋节：2024-09-15至2024-09-17（共3天）
- 国庆节：2024-10-01至2024-10-07（共7天）

## 2025年节假日
- 元旦：2024-12-30至2025-01-01（共3天）
- 春节：2025-01-28至2025-02-03（共7天）
- 清明节：2025-04-04至2025-04-06（共3天）
- 劳动节：2025-05-01至2025-05-03（共3天）
- 端午节：2025-05-31至2025-06-02（共3天）
- 中秋节：2025-10-05至2025-10-07（共3天）
- 国庆节：2025-10-01至2025-10-07（共7天，可能与中秋节衔接调休）

## 2026年节假日
- 元旦：2026-01-01至2026-01-03（共3天）
- 春节：2026-02-17至2026-02-23（共7天）
- 清明节：2026-04-04至2026-04-06（共3天）
- 劳动节：2026-05-01至2026-05-03（共3天）
- 端午节：2026-06-19至2026-06-21（共3天）
- 中秋节：2026-09-25至2026-09-27（共3天）
- 国庆节：2026-10-01至2026-10-07（共7天）

## 节假日说明：
1. 以上日期均为法定节假日放假时间
2. 春节和国庆节通常有调休，实际放假天数可能包含周末
3. 部分年份中秋节和国庆节相邻或重叠，放假时间会合并安排
4. 劳动节假期在2020年之后调整为5天
5. 春节假期通常从除夕开始，到初六结束

如果所有规则均未触发：直接返回原始解析结果，新增 res 字段说明未触发原因

严格限制：除非明确触发修正规则，否则禁止对 target_time_range 做任何修改
```

```plsql
{query}
```

### SQL生成（LLM）

```plsql
你是数仓专家，请基于 用户输入 和 知识库内容 生成高性能、可直接执行的 ODPS SQL。请严格遵循以下规则：


### 输入
用户查询：${{input}}
知识库内容:
{{strategy_info}}
{{push_info}}
{{delivery_info}}
{{user_profile1}}
{{user_bot_input}}


昨天日期：
 {{TEMPLATE_RESULT}}

一、基础生成规则（必须100%遵守）
1.字段与值标准化
所有表名、字段名、枚举值、时间字段必须使用「映射结果」中的「映射后」标准内容，禁止保留用户原始表述（如"海外feeds页"必须转为 space_name = '海外版首页feeds'）。
指标计算必须使用「取数口径」指定的聚合函数（如 SUM(expo_pv_1d)），指标别名必须与「指标名」完全一致（如 曝光pv）。
2. dt 分区字段处理（按优先级严格判断）
✅ 优先级1：时序对比识别
若 {{user_bot_input}}  明确包含"同比"、"环比"、"相较"、"对比"、"上周"、"上月"等对比关键词，或 {{input}}的time_range_type = '时间对比'：
→ 必须构造双子查询结构；
→ 每个子查询应该做分区剪裁，避免全表扫描
→ 禁止使用 max_pt()。
✅ 优先级2：数据表名是_dd结尾的表兜底策略
若查询表名以 _dd 结尾（表示日粒度分区表），且未命中优先级1：
→ 必须忽略时间范围中的 start_date/end_date，强制使用 dt = max_pt('完整表名')。
✅ 优先级3：默认使用 {{input}}时间区间


3.输出格式
仅返回可执行 SQL 语句本身；
不含注释、分号、解释文字或 Markdown。


二、性能与结构优化规则
1.分区裁剪
 ○ dt 条件必须置于 WHERE 最前；
 ○ 日期格式必须为 'YYYYMMDD' 字符串；
 ○ max_pt() 必须带完整表名（如 max_pt('anthk_sg.dws_hk_asap_mkt_distribute_index_1d')）；
2.过滤条件顺序
 ○ dt = ... → 高基数等值字段（如 space_name, target_area_name）→ 其他字段。
 ○ 所有字符串值用单引号包裹（如 '海外'），禁止对字段使用函数或隐式转换。
3.聚合与维度
 ○ SELECT 仅包含必要字段：聚合指标 + 维度字段；
 ○ 有维度则 GROUP BY 字段必须与 SELECT 中维度完全一致；
 ○ 直接使用预聚合字段（如 expo_pv_1d），禁止二次计算。
4.TOP N 结果限制
  ○ 若 {{input.raw_text}} 包含以下任一语义：
    极值词： "最高"、"最低"、"最大"、"最小"、"最好"、"最差"、"第1高"、"最多"、"top 1"、"最少"
    单结果问法： "是什么"、"哪一条"、"哪个内容"→ 必须生成 LIMIT 1（这是硬性业务规则，不可兜底为 100/1000）。
  ○ 若包含 "top N"、"前N"、"最多N个" 、 "topN"、  "前N"、"后N"→ 使用用户指定的 N 值；
  ○ 必须根据语义决定排序方向：
    "最高"、"最大" 、"最多"、"前N"→ ORDER BY 指标 DESC
    "最低"、"最小"、"最少" 、"后N" → ORDER BY 指标 ASC
  ○ LIMIT 必须置于 SQL 末尾，且仅出现一次。
   示例："目前曝光top40的内容" → 视为含"目前"（时间词）+ "top40"（排名语义）。
5.时序对比结构（如适用）
 ○ 使用双子查询 + LEFT JOIN；
 ○ 子查询别名禁用 SQL 保留字（推荐 curr/prev 或 t1/t2）；
 ○ 指标在子查询内完成聚合与安全计算（如 NULLIF）；
 ○ 最终输出包含当前值 + 增长率（ROUND((curr - prev) / NULLIF(prev, 0), 6)）。
6. JOIN 场景下的分区裁剪规则（高性能核心）
 ○ 任何涉及多表 JOIN 的查询，必须为每个表单独构造子查询，并在子查询内完成以下操作：
 ○ 首行写 dt = 'YYYYMMDD' 或 dt = max_pt('完整表名')；
 ○ 包含该表所需的所有等值过滤条件（如 space_name = '...'）；
 ○ 仅 SELECT 必要字段（如 user_id），避免宽表传输；
 ○ 禁止在主 WHERE 中统一写 a.dt = ... AND b.dt = ...，因其无法保证分区裁剪下推；
 ○ 子查询别名简洁（如 a/b/t1/t2），避免保留字。

 三、模糊匹配处理规范
  1.模糊语义识别与字段类型强校验
当 {{user_bot_input}} 或过滤值中出现以下表达时，必须同时满足两个条件才能生成 LIKE：
(a) 语义匹配：
前缀匹配：如"86开头"、"以86开始" → 提取核心值 '86%'；
后缀匹配：如"以@qq.com结尾" → 提取 '@qq.com' → 生成 '%@qq.com'。
(b) 字段类型校验：
仅当目标字段在知识库中被明确定义为字符串类型（如 register_number、email、phone 等含"号码""注册号""邮箱"描述的字段）时，才允许生成 LIKE；
若字段为枚举型或数值型（如 user_type、user_age、is_ph_maid_flag），即使用户输入"86开头"，也视为无效请求，返回"目前用户查询无法满足"。

2.性能与安全监控要求
✅ 允许：前缀匹配 LIKE 'xxx%'（ODPS 可下推）；
⚠️ 禁止：中缀/后缀模糊 LIKE '%xxx' 或 '%xxx%'；
❌ 禁止：使用 RLIKE、正则表达式，无论场景。

四、典型示例（供参考）
raw_text = "百岁老人用户数"
→ 无时间词 → dt = max_pt(...)
sql
SELECT COUNT(user_id) AS 用户数 FROM antsg_asap.adm_asap_base_user_label_dd WHERE dt = max_pt('antsg_asap.adm_asap_base_user_label_dd') AND user_age = '100'
raw_text = "昨天首页feeds的pvctr"
→ 含"昨天" → dt = '20260108'
sql
SELECT SUM(clk_pv_1d) * 1.0 / NULLIF(SUM(expo_pv_1d), 0) AS pvctr FROM anthk_sg.dws_hk_asap_mkt_distribute_index_1d WHERE dt = '20260108' AND space_name = '首页feeds'
raw_text = "目前海外feeds页曝光top40的内容"
→ 含"目前"（视为时间词，但未指定具体日）+ "top40" → 注意：若"目前"未映射到具体日期，则视为无明确时间词，应使用 max_pt()；但若 RAG 映射提供了具体日期（如 20260113），则用该日期。
→ 同时需 ORDER BY + LIMIT N(用户指定或默认)。

四、最终输出要求
所有的维度和指标必须来自{{rag_info}}已有的内容，禁止自己造维度，如果无法生成准确的sql，请返回 目前用户查询无法满足及运营；
仅返回一条符合上述所有规则的 ODPS SQL，确保可直接在生产环境执行；
最终输出只能是以下两种之一：(a) 一条合法的 ODPS SQL；(b) 字符串"目前用户查询无法满足"，并说明原因。
```

```plsql
{query}
```

### SQL校验（LLM）

```plsql
你是一个专业的 ODPS（MaxCompute）SQL 校验与改写器。
请根据以下两个输入进行分析与优化：

用户自然语言查询：{{input}}
当前生成的 ODPS SQL：{{sql}}

你的任务是：严格校验并改写该 SQL，并完全符合 ODPS 语法规范。
重点聚焦 LIMIT 逻辑、排序方向、LIKE 模糊匹配 ，维度指标合理性四大维度

✅ 1. LIMIT 与排序方向硬性规则（必须遵守）
⚠️ 以下规则具有最高优先级，覆盖所有默认或模糊判断逻辑

(1) 单结果强制 LIMIT 1
若 {{input}} 包含以下任一语义，必须生成 LIMIT 1：
极值词：
"最高"、"最低"、"最大"、"最小"、"最好"、"最差"、"最多"、"最少"、"第1高"、"top 1" 等
单结果问法：
"是什么"、"哪一条"、"哪个内容"、"具体是哪个"、"唯一结果" 等
�� 这是硬性业务规则：即使原 SQL 未包含 LIMIT 或包含错误数量，必须修正为 LIMIT 1

(2) Top-N / 前N / 后N 语义识别
若用户输入包含：

"top N"、"前N"、"最多N个"、"topN"、"前N条"、"后N"、"倒数N" 等（N 为数字）
→ 提取 N 值，生成 LIMIT N
→ 若 N 无法解析（如"前几条"），视为模糊请求，默认 LIMIT 10 并加注释

(3) 排序方向必须与语义一致
升序（ASC）场景：
"最低"、"最小"、"最少"、"后N"、"倒数N" → ORDER BY [指标] ASC
降序（DESC）场景：
"最高"、"最大"、"最多"、"最好"、"前N"、"top N" → ORDER BY [指标] DESC
�� 若原 SQL 缺少 ORDER BY，但语义要求极值或 Top-N，则必须推断合理排序字段（如数值列、时间列）并添加 ORDER BY
�� 若排序方向与语义冲突（如"最高"却用 ASC），必须修正



✅ 2. LIKE 逻辑校验（ODPS 兼容）
用户说"以 X 开头" → LIKE 'X%'；"以 X 结尾" → LIKE '%X'
若 LIKE 模式以 % 开头 → 添加注释：-- ⚠️ 前导通配符可能导致性能下降
大小写不敏感需求 → 使用 UPPER(col) LIKE UPPER('...') 并注明


✅ 3. 指标合理性
所有维度必须属于知识库维度字段，不能自己臆想捏造
指标计算口径需要符合知识库内容，如果不准确需要调整准确

✅ 4. ODPS 语法与安全规范
所有表名、列名用反引号 ` 包裹
LIMIT 必须位于语句末尾，且不能出现在子查询中（除非使用窗口函数）
避免 SELECT * 在生产环境？→ 保留 * 若用户未指定字段，但可加注释建议显式列名
移除 MySQL/PostgreSQL 特有语法（如 LIMIT offset, count → ODPS 仅支持 LIMIT count）
无 ORDER BY 的 LIMIT 视为无效/危险 → 必须补充合理排序或移除 LIMIT

✅ 5.输出格式要求
仅输出改写后的 ODPS SQL 语句
可包含 -- 注释，用于说明：
为何添加/修正 LIMIT 1（引用极值词或单结果问法）
排序字段与方向的推断依据
LIKE 模式修正或性能提示
禁止输出任何解释、推理过程、JSON、额外文本
```

```plsql
{query}
```

# 后续优化方案
## 意图理解层
+ **语义澄清机制**：当用户输入模糊时（如"任务ID"），主动引导明确实体类型（投放任务？策略任务？）；
+ **多主题域联合识别**：支持跨域条件组合（如"曝光 + push"）；
+ **时间-行为解耦**：区分"时间范围"与"行为中的时间属性"（如"最近7天登录" vs "注册距今7天"）。

## 知识召回层
+ **意图驱动召回**：将意图解析出的关键词（如"eM+"、"港漂"）作为召回增强信号；
+ **枚举值标准化**：建立业务术语 ↔ 标准字段/值的映射词典（如"年轻人" → `user_age_group IN ('2','3','4')`）。

## SQL 生成层
+ **禁止伪造**：无匹配字段/值时，返回"目前用户查询无法满足"；
+ **支持高级语法**：
    - `LIKE`（前缀匹配，如"86开头"）；
    - `GROUP BY + HAVING`（如"活跃≥3个场景的用户数"）；
    - 安全除法（`NULLIF`）；
+ **性能兜底**：自动添加 `dt = max_pt(...)` 或分区裁剪。

## 系统稳定性
+ **超时控制**：单节点执行超时熔断；
+ **Token 限制**：对长上下文进行截断或分片处理；
+ **错误分类上报**：区分"知识缺失"、"语义模糊"、"生成错误"等根因。

## 总结与建议
**短期目标**：聚焦单主题域，打造高可靠 NL2SQL 能力；  
**长期目标**：通过语义澄清 + 知识增强 + 规则引擎，突破 80% 准确率天花板。



当前主要挑战包括：

+ 用户表述模糊、口语化严重；
+ 多主题域、多条件混合查询；
+ 时间语义嵌套复杂；
+ 知识库覆盖不全导致字段/值伪造。
